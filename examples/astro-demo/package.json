{
  "name": "@flowmark/astro-demo",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile:templates": "node scripts/compile-flow.mjs",
    "predev": "pnpm run compile:templates",
    "dev": "astro dev",
    "dev:e2e": "pnpm run compile:templates && astro dev --host 127.0.0.1 --port 4321",
    "prebuild": "pnpm run compile:templates",
    "build": "astro build",
    "preview": "astro preview",
    "test": "pnpm run test:unit",
    "test:unit": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@andersseen/layout": "^0.0.1",
    "@andersseen/motion": "^0.1.1",
    "@andersseen/web-components": "^0.0.8",
    "@flowmark/runtime": "workspace:*",
    "astro": "^5.7.10"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@tailwindcss/vite": "^4.3.1",
    "tailwindcss": "^4.3.1",
    "vitest": "^3.2.6"
  }
}
